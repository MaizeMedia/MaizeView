{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the catalog (main) window",
  "windows": ["catalog", "main"],
  "permissions": [
    "core:default",
    "core:event:default",
    "core:webview:allow-create-webview-window",
    "opener:default",
    "dialog:default"
  ]
}
